services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: danangtrip_app
    ports:
      - "8000:8000"
    environment:
      PORT: 8000
    volumes:
      - ../:/var/www
    working_dir: /var/www
    env_file:
      - ../.env
    dns:
      - 8.8.8.8
